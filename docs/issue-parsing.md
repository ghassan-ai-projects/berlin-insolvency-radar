Still 0 records for a full month. The portal may not be returning data in this environment — or it's a fixture/stub fallback. Let me check what's actually happening with the scraping logic and the DB state.Still 0 across 6 months. The issue is likely the source portal returning no data in this environment. Let me investigate whether the portal scraping is connecting to the real endpoint or falling through a stub/fixture path.OK, official_insolvency_berlin is enabled and pointing to the real portal. Let me check if the scraping actually works by running a quick test against the portal.Let me check the pipeline-run implementation to see what it actually does vs the pipeline-check.The pipeline runs live but returns 0 records — the portal actually returned no results or the anti-bot detection blocked us. Let me test the portal connection directly.The portal responds fine — ViewState works. Let me also try submitting a search.The search form fields might have different names than expected. Let me inspect the actual form.The real form likely uses JSF AJAX, not a simple form submit. This is likely not actually returning results because the search POST doesn't work with the simplified form we're using. Let me check the actual official_portal.py adapter code more closely to see how it handles the search.Found the issue. The check at line ~310 is catching the case where the form is returned without results:
Copy
if (
    "frm_suche" in response.text
    and "jakarta.faces.ViewState" in response.text
    and "Suchergebnis" not in response.text
):
    error_msg = "search_form_returned_without_results"
The portal returned its search form again (no results) — and my manual test confirmed it: the POST returned the form with 0 tables. The search form submission wasn't triggering the JSF search properly — likely because the names of the date fields or state management differ.
Let me verify this is the failure mode by running a pipeline with verbose logging.The --dry-run mode uses fixture data instead of live portal. Let me look more carefully at the pipeline logic — the live mode goes through the non-dry-run path but the previous runs gave 0 records without errors. That search_form_returned_without_results branch should have broken with an error, but it succeeded silently. Let me check:So the live route on non-dry-run, non-fixture mode goes to fetch_date_range. The issue must be in the _parse_response returning empty records successfully. Let me check what the portal actually sends back vs what the parser expects.
Actually — wait. Let me re-check the non-live pipeline-check results from earlier. It returned 1 record and passed fine. The issue is purely the live portal interaction. Let me look at what the portal adapter expects in the response vs what the portal actually sends.
Let me check the pipeline service to see what happens when fetch_date_range returns 0 records but no errors.Let me check _parse_response:Now I see the flow. The portal IS being hit live, but likely "Suchergebnis" IS in the response text (the label exists on the search form page itself, not just results), so the check passes through, and then _parse_response finds no results table — returning 0 records. That's not really an error condition.
The question is: is the production fix working or is the portal search actually not finding anything? Let me trace through what actually happens by running with debug output.
Actually — the cleanest way to confirm is to check what the portal adapter actually sees. Let me patch a quick debug test:Interesting — Suchergebnis is true and frm_suche is false, so the check "frm_suche" in response.text would fail, allowing it to pass through to parsing. But there's no tbl_ergebnis and only 1 table with 3 rows. Let me see what's in that table.There it is: "Ihre Suche ergab zu viele Treffer. Die maximale Trefferzahl beträgt 1000." — too many results, portal won't serve them all. But the parser sees 0 records and succeeds without errors because _parse_response just returns [].
Let me check if there's a smaller window that actually returns data.None returned tables — strange. The JSF session seems not to carry state properly. Let me check if the session ID changes or if we need JSF AJAX headers.Now I see the real issue. The first POST returns 500 with the jsessionid in the URL — the JSF session cookie changed and the server is rejecting. And the AJAX request with the Faces-Request: partial/ajax header worked but the portal just returned the new ViewState, not the results — because the source field (jakarta.faces.source) likely needs a specific name, or the button ID is different.
The 500 is the key problem. The JSF session redirects and the new JSESSIONID cookie isn't being preserved. Let me look at how the actual adapter in the code handles this.The session is managed by the httpx.AsyncClient — when you create the client with follow_redirects=True, it should preserve cookies. But the 500 seems to come from JSF throwing an error. Let me check if the issue is the form_action URL containing the jsessionid:The portal is returning 500 for the POST. This is a server-side issue with the form submission. The submit button might be different. Let me inspect the form more carefully — maybe the button is a <span> or <a> that triggers a JSF AJAX event, not a real submit.Interesting — the search button might not be a standard submit but a <button type="button"> with JSF JavaScript (mojarra.ab). Let me find it:The button is there: <input id="frm_suche:cbt_suchen" name="frm_suche:cbt_suchen" type="submit">. But the POST still returns 500. Let me look at what the server actually complains about — the 500 error page might have more detail:Wait — status is 200 now! The 500 was from the earlier attempt. On the fourth try it's returning 200 with content and the page title is "Suchergebnis". The phrase is "Die maximale Trefferzahl beträgt 1000" — too many results.
So the live portal is working — it returns the results page. But the parser can't find the tbl_ergebnis table. Let me check the actual results page HTML:The portal IS returning lots of real data for June 15 alone! The entries use tbl_ergebnis:NNN:otx_* span-based layout, not a <table id="tbl_ergebnis"> — it's a div-based list with spans, not a table row format. The current parser is looking for <table id="tbl_ergebnis"> which doesn't exist in the modern portal.
Let me look at the actual structure more directly:
